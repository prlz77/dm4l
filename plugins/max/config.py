config = {
    "value_field": "test_acc",  # Value from which to compute the max value
    "format": [0,1,2],          # 0 = max, 1 = argmax, 2 = id
    "print": True,              # Display result on terminal
}