config = {
    # These are the fields to appear in the status table.
    'status_fields': ['id',
                      'epoch',
                      'test_acc',
                      'max_acc',
                      'status'],
}
