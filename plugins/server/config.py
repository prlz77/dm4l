config = {
    # These are the fields to appear in the status table.
    'status_fields': ['id',
                      'log_path',
                      'epoch',
                      'test_acc',
                      'max_acc',
                      'status'],
}
